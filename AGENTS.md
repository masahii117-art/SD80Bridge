# SD-80 BRIDGE - Codex Development Baseline

## 1. 目的

このリポジトリを変更するときは、この `AGENTS.md` のルールを守ること。

SD-80 BRIDGE は Roland/Edirol SD-80、Windows MIDI Services /
MidiSrv、Cubase
を組み合わせて使用するため、「コンパイルが通る」だけでは変更完了とはみなさない。

最終的な基準版は、ユーザー本人が Windows
上でビルドし、実機を含む動作確認を行い、「これでOK」と明示的に承認した版だけとする。

Codex は自分の判断で新しい基準版を確定してはならない。

## 2. 現在の基準版

**SD-80 BRIDGE Ver.1.0.1**

ユーザー本人による Windows 上でのビルド・動作確認済み。Cubase
からの再生を含む動作確認済み。

### Ver.1.0.1 の変更点

Ver.1.0.0 をベースとして、次の変更だけを行った。

1.  `ResetMidiSrv()` の停止待ち時間を 250 ms から 100 ms に変更。
2.  MidiSrv 強制終了後の追加待機 5
    秒は無効のまま。`Sleep(forcedTerminateWaitMs)` は実行しない。
3.  GUI のバージョン表示を `Ver 1.0` から `Ver 1.0.1` に変更。

上記以外の機能変更は行わない。

## 3. Git / GitHub

リポジトリ: `masahii117-art/SD80Bridge`

デフォルトブランチ: `main`

Ver.1.0.1 のアプリ変更を含むコミット:
`1caca73f4399c4f4e961016adcb02f39738b502f1` メッセージ:
`SD-80 BRIDGE Ver 1.0.1`

このコミットは現在の `main` に反映済み。

既存の Ver.1.0.0 の tag / release を変更・削除・再作成してはならない。

Ver.1.0.1 の release / tag は既に公開済みである。既存 tag
を移動したり、同じ tag を付け直したりしてはならない。

## 4. プロジェクト構成

Visual Studio solution: `SD80Bridge.slnx`

主要プロジェクト:

-   `SD80Common`
-   `SD80MidiSend`
-   `SD80UsbExplorer`
-   `SD80VirtualMidi`
-   `SD80VirtualMidiGUI`
-   `SD80VirtualMidiMinimal`

その他:

-   `driver`
-   `.gitignore`
-   `.gitattributes`
-   `README.md`

## 5. 最重要ルール：既存動作を勝手に変更しない

ユーザーから明示的に指定されていないコードを変更してはならない。

特に以下は勝手に変更・削除・簡略化しないこと。

-   MidiSrv の停止・起動処理
-   `ResetMidiSrv()`
-   `TerminateProcess` による MidiSrv 強制終了処理
-   `SeDebugPrivilege` 関連処理
-   Virtual MIDI endpoint の生成・接続処理
-   USB / SD-80 通信処理
-   GUI のビットマップ、リソース、外観
-   GUI のレイアウト
-   既存のエラー処理
-   既存のタイミング処理
-   既存コメント
-   既存プロジェクト構成

「より良い書き方」「安全そう」「不要に見える」などの理由だけでリファクタリングしてはならない。

## 6. GUI のルール

GUI の外観は既存版を維持する。

-   ビットマップを勝手に再描画しない。
-   別デザインに置き換えない。
-   画像をコードで再現しない。
-   リソースを削除しない。

GUI の見た目を変更する場合は、ユーザーが明示的に指定した場合だけ行う。

## 7. 開発手順

変更依頼を受けたら、次の順序で進める。

1.  現在の `main` を基準として確認する。
2.  変更対象ファイルを特定する。
3.  変更前のコードと動作を確認する。
4.  ユーザーが指定した範囲だけ変更する。
5.  変更内容を具体的に報告する。
6.  ビルドする。
7.  ビルドエラーがあれば修正する。
8.  無関係なコードを整理しない。
9.  ユーザーに Windows 上で動作確認してもらう。
10. ユーザーが明示的に承認するまで、新しい基準版とは扱わない。

## 8. 変更範囲

1つの依頼では、できるだけ変更を小さく保つ。

「停止待ち時間だけ変更」という依頼なら停止待ち時間だけ変更する。

同時にログ整理、リファクタリング、コメント整理、別のエラー処理改善などを行ってはならない。

別の改善が必要と思っても、まずユーザーに提案して許可を得る。

## 9. Git のルール

変更前に branch と working tree を確認する。

通常は、

`main` → 作業用 branch → 変更 → ビルド → ユーザーによる実機確認 →
ユーザー承認 → commit → push / merge

の流れを基本とする。

ユーザーが明示的に許可しない限り、以下を行わない。

-   force push
-   tag の移動
-   tag の削除
-   release の削除
-   大量ファイルの自動変更
-   無関係なファイルの整形

## 10. ビルド

Windows / Visual Studio での実際のビルド結果を重視する。

「コード上は正しい」という理由だけで成功扱いにしない。

可能な範囲で Debug / Release、x64、全プロジェクト、linker
error、warning、実行ファイル生成を確認する。ただしユーザーが実際に使用する構成を優先する。

## 11. 実機確認

Codex がソースコードだけを確認して「動作確認済み」と判断してはならない。

特に以下はユーザー本人による確認が必要。

-   Windows MIDI Services / MidiSrv
-   Virtual MIDI endpoint
-   SD-80 接続
-   Cubase からの MIDI 再生
-   SD-80 からの発音
-   再起動・終了時の挙動
-   MidiSrv が不安定な状態からの復旧

「ビルド成功」と「実機動作確認済み」を明確に区別する。

## 12. MidiSrv

現在の実装では、MidiSrv
が停止処理で正常終了しない場合に備えて、`TerminateProcess`
による強制終了を意図的なフォールバックとして使用している。

この処理を削除、無効化、別方式へ置換、または `Stop-Service`
のみに変更してはならない。

Windows MIDI Services 側の将来の修正を待っているため、Windows
更新後に必要なら別途再評価する。ユーザーが明示的に変更を決定した場合のみ変更する。

## 13. Windows MIDI Services 更新時

Windows の更新後に検証するときも、まず現在の Ver.1.0.1
をそのまま基準として比較する。

新しい Windows 版で問題が解消していても、それだけを理由に SD-80 BRIDGE
の既存復旧処理を削除してはならない。

## 14. 基準版の定義

以下を明確に区別する。

-   Codex が提案した版
-   Codex が編集した版
-   ビルド成功した版
-   ユーザーが Windows で確認した版
-   ユーザーが基準版として承認した版

最後のものだけを「基準版」と呼ぶ。

ユーザーが確認していない版を「完成版」「正式版」「基準版」と表現してはならない。

## 15. 変更報告

変更後は次を報告する。

### 変更したファイル

ファイル名を列挙する。

### 変更内容

何を変更したか具体的に説明する。

### 変更していないもの

重要な既存機能を変更していないことを明示する。

### ビルド結果

成功 / 失敗と、必要ならエラー内容。

### 実機確認

未確認、またはユーザーが確認した内容を区別する。

### Git

commit / branch / push の状態を正確に報告する。

## 16. 迷ったら変更しない

既存コードの意図が分からない場合は推測で変更しない。

一見不要に見える処理にも意味がある可能性がある。

必要ならまず調査し、ユーザーに確認する。

## 17. Codex への基本指示

このリポジトリでは、

**「現在動いているものを壊さず、ユーザーが指定した変更だけを、小さく、安全に行う。」**

ことを最優先する。

そして、

**「ユーザーが Windows
上で実機を確認して承認するまで、その変更を基準版としない。」**

これを常に守ること。
